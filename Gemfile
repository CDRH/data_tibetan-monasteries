source "https://rubygems.org"

gem 'datura', git: 'https://github.com/CDRH/datura', tag: "v1.1.0"
gem 'byebug'

# Will said this was added when working with an error
# Commenting out, but if it needs re-enabled also change the gemset
# to `datura_dev` as it will be a different Gemfile from other repos
#gem 'fileutils'

gem 'pdf-reader'

